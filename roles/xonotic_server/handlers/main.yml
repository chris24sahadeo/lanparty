---
- name: Reload systemd
  ansible.builtin.systemd_service:
    daemon_reload: true
  become: true

- name: Restart the Xonotic dedicated server
  ansible.builtin.systemd_service:
    name: "{{ game_service_name }}"
    state: restarted
  become: true
